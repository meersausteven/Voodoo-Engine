
class Range {
        min;
        max;
        step;

        constructor(min, max, step) {
                // Number min: minimum value
                // Number max: maximum value
                // Number step: step size of increase/decrease

                this.min = min;
                this.max = max;
                this.step = step;
        }
}