
class Renderer {
        constructor(parent, offsetX, offsetY) {
                this.parent = parent;
                this.offset = {
                        x: offsetX,
                        y: offsetY
                };
        }

        update() {
                return;
        }

        render() {
                return;
        }

        showBounds() {
                return;
        }
}
